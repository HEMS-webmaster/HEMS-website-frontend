# Goal: GCloud Link Preview-on-Hover Integration

The objective is to introduce a "preview-on-hover" functionality for all GCloud-hosted PDF links rendered on the workshop archive frontend. This provides an immediate, inline visual or text preview (thumbnails for presentations/programs and text blurbs for abstracts) without requiring the user to download or open the file.

## User Review Required
> [!IMPORTANT]
> The proposed `FrontendPreviewHover` component will rely on the `_preview.png` and `_preview.txt` files generated by the ingest pipeline, loading them directly from the provided GCloud URL or the local serve route. Please confirm if this approach aligns with your expectations before I execute the changes.

## Proposed Changes

---

### Shared UI Components

#### [NEW] `src/frontend/src/components/FrontendPreviewHover.tsx`
- Build a new reusable client component that wraps any React node (e.g., an `<a>` tag).
- Extract the base URL from the `href` prop.
- If the URL targets a `.pdf`, determine whether it is an abstract (using string matching for `_Abstract`).
- Swap the `.pdf` extension for `_preview.png` or `_preview.txt` respectively.
- On hover:
  - If it is an abstract, `fetch()` the text and render it within an absolute hover card.
  - If it is an image, render an `<img>` tag pointing directly to the preview URL.
- Handle loading states and gracefully fall back to just the children if a preview cannot be found.

---

### Backend API: Local File Server

#### [MODIFY] `src/frontend/src/app/api/manager/serve/route.ts`
- Update the MIME type routing logic to explicitly support `.txt` (`text/plain`). This ensures that local development environments can successfully fetch and parse the generated text previews.

---

### Frontend UI: Archive Template

#### [MODIFY] `src/frontend/src/app/archive/[year]/page.tsx`
- Import `FrontendPreviewHover`.
- Wrap all anchor tags (`<a>`) that render `public_website_url` or `local_target_path` within the new component.
- Affected sections:
  - Workshop Resources (Program, Participant List)
  - Technical Program (Presentations and Abstracts)
  - Posters (Posters and Abstracts)
  - Student Awards (Awards and Abstracts)

## Verification Plan

### Automated Tests
- Build verification (`npm run build`) to ensure no TypeScript or Next.js route errors.

### Manual Verification
- Test the hover states locally by navigating to `localhost:3000/archive/14th`.
- Verify that hovering over a presentation or program loads the `.png` thumbnail.
- Verify that hovering over an abstract correctly `fetch()`es and displays the `.txt` content.
